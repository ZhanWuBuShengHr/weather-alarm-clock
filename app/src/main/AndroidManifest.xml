<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 权限声明 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherAlarmClock"
        tools:targetApi="31">
        
        <!-- 主Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WeatherAlarmClock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 闹钟设置Activity -->
        <activity
            android:name=".ui.AlarmSettingActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherAlarmClock" />

        <!-- 图标选择Activity -->
        <activity
            android:name=".ui.IconSelectActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherAlarmClock" />
            
        <!-- 帮助Activity -->
        <activity
            android:name=".ui.HelpActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherAlarmClock" />

        <!-- 闹钟响铃Activity -->
        <activity
            android:name=".ui.AlarmRingActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherAlarmClock.NoActionBar"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

        <!-- 闹钟服务 -->
        <service
            android:name=".service.AlarmService"
            android:enabled="true"
            android:exported="false" />

        <!-- 开机自启动接收器 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>